# Optional — used for future backend integrations
VITE_APP_NAME=Portfolio CI/CD
VITE_CONTACT_EMAIL=alex.morgan@email.com

# GitHub Actions secret (not in .env — set in repo Settings → Secrets)
# RENDER_DEPLOY_HOOK=https://api.render.com/deploy/srv-xxx?key=xxx
